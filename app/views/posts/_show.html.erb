<!-- Temporary to remove duplicate submitted by user lines --> 
<% if @post.title.blank? %>

<% else %>
	<h2>
	  <%= @post.title %>
	</h2>
	<p>
		<%= @post.content %>
	</p>
	<p style="font-style: italic;color:#999">
	  submitted by <%= @post.user.name %>
	</p>
<% end %>
